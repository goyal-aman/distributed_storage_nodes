info:
  name: get replication
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:7770/v1/data/replicate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
