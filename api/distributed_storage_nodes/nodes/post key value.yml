info:
  name: post key value
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7770/v1/data
  body:
    type: json
    data: |-
      {
        "key":"1",
        "value":"1"
      }  
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
