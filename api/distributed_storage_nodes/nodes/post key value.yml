info:
  name: post key value
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7771/v1/data
  body:
    type: json
    data: |-
      {
        "key":"name",
        "value":{
          "userid":"aman",
          "usertype":"developer"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
