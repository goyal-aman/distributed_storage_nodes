info:
  name: key value
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:7770/v1/data?writequorum=-1&include=res
  params:
    - name: writequorum
      value: "-1"
      type: query
    - name: include
      value: res
      type: query
  body:
    type: json
    data: |-
      {
        "key":"1",
        "value":"1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
